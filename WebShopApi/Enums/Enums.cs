namespace WebShopApi.Enums
{
    public static class Enums
    {
        public enum UserType
        {
            Admin = 0,
            Customer = 1,
            Seller = 2,
        }

        public enum VeryfiedType
        {
            Approved = 0,
            Denied = 1,
            InProgress = 2,
        }
    }
}
